import React from 'react';
import { storiesOf } from '@kadira/storybook';

// Commented out because the scoring module requires the Sketches collection, which
// doesnt play well with Storybook.
//
// import ScoringTuning from '/imports/ui/components/ScoringTuning.jsx';
// storiesOf('Scoring', module)
//   .add('stars = fn(rank, confidence)', () => (
//     <ScoringTuning />
//   ));
